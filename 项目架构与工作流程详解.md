# 科研灵感助手 Agent — 项目架构与工作流程详解

> 本文档从宏观架构和微观细节两个层面，完整介绍整个项目的运行机制。

---

## 一、宏观架构：四大层

```
┌─────────────────────────────────────────────────────────┐
│                   表现层 (Presentation)                    │
│                  Streamlit (app.py)                       │
│   聊天界面 · 知识库管理 · 会话管理 · 日志面板              │
├─────────────────────────────────────────────────────────┤
│                   业务逻辑层 (Business Logic)              │
│                                                         │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│   │ agent_core   │  │  tools.py   │  │paper_search │     │
│   │  ReAct 循环   │  │ 知识库检索   │  │  _mcp.py   │     │
│   │ 上下文编排    │  │ (RAG Tool)  │  │ 联网搜索    │     │
│   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘     │
│          │                │                │             │
│   ┌──────┴────────────────┴────────────────┴──────┐     │
│   │              llm_service.py                    │     │
│   │         Qwen3-max (DashScope API)              │     │
│   └───────────────────────────────────────────────┘     │
├─────────────────────────────────────────────────────────┤
│                   知识层 (Knowledge)                     │
│                                                         │
│   ┌──────────┐  ┌──────────┐  ┌──────────────────┐     │
│   │ retriever │  │ reranker │  │ query_rewriter   │     │
│   │ BM25+语义  │  │CrossEnc  │  │ LLM 查询变体生成  │     │
│   └────┬─────┘  └──────────┘  └──────────────────┘     │
│        │                                                │
│   ┌────┴─────┐  ┌──────────────┐  ┌───────────────┐    │
│   │ Chroma   │  │document_proc │  │embedding_svc  │    │
│   │ 向量存储  │  │ Docling PDF  │  │ DashScope emb │    │
│   └──────────┘  │ → MD 分块    │  └───────────────┘    │
│                 └──────────────┘                        │
├─────────────────────────────────────────────────────────┤
│                   数据层 (Data)                           │
│                                                         │
│   ┌──────────────────────────────────────────────┐     │
│   │              MySQL (SQLAlchemy ORM)            │     │
│   │  sessions · messages · summaries               │     │
│   │  documents · chunks                            │     │
│   └──────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────┘
```

**注意**：本项目**没有** FastAPI 或任何 HTTP 服务端。Streamlit 本身就是完整的 Web 服务器——它在前端起了一个 Python 进程，所有 Python 代码（包括 Agent、LLM 调用、数据库读写）都**在同一进程内**直接调用，不经过 HTTP/RPC。

---

## 二、核心文件职责速览

| 文件 | 一句话职责 |
|------|----------|
| `app.py` | Streamlit 前端：聊天 UI + 知识库上传 + 会话管理 + 日志面板 |
| `agent/agent_core.py` | ReAct Agent 主循环：收消息 → 调 LLM → 执行 Tool → 生成回复 |
| `agent/llm_service.py` | 封装 Qwen3-max API 调用（OpenAI 兼容协议），含重试 + 日志 |
| `agent/tools.py` | 定义 `search_knowledge_base` 工具（本地 RAG 检索） |
| `agent/paper_search_mcp.py` | 定义 `search_papers_online` 工具（联网论文搜索，MCP 风格） |
| `agent/middleware.py` | 工具调用追踪：记录每次 Tool 调用的参数、耗时、结果 |
| `knowledge_base/retriever.py` | 混合检索引擎：BM25 + 语义检索 + RRF 融合 |
| `knowledge_base/reranker.py` | Cross-Encoder 重排序（BGE-reranker-large） |
| `knowledge_base/query_rewriter.py` | LLM 查询改写：将用户问题拆解为多个同义词变体 |
| `knowledge_base/document_processor.py` | Docling PDF → Markdown + 按 ## 章节标题分块 |
| `knowledge_base/embedding_service.py` | DashScope text-embedding-v4 向量化 |
| `knowledge_base/vector_store.py` | Chroma 向量存储（持久化） |
| `knowledge_base/paper_search.py` | 三源论文检索：arXiv + OpenAlex + Semantic Scholar |
| `session/session_manager.py` | 会话 CRUD + 上下文构建 + 消息管理 |
| `session/summarizer.py` | 自动摘要：超过 20 条消息触发 LLM 生成摘要 |
| `database/connection.py` | SQLAlchemy 引擎 + Session 工厂 |
| `database/models.py` | ORM 数据模型（5 张表） |
| `config.py` | 全局配置：API Key、MySQL 参数、分块大小、检索参数 |
| `utils/logger.py` | 统一日志系统：控制台彩色 + 文件 + 内存环形缓冲 |
| `utils/helpers.py` | 工具函数：MD5、token 估算、trace_id 生成 |

---

## 三、业务全流程：用户一次提问的完整生命周期

假设用户在聊天框输入：

> "有没有使用 wav2vec 来做部分伪造音频检测的论文？5 篇就行"

### 第 1 步：前端接收 (app.py)

```
st.chat_input → prompt = "有没有使用 wav2vec..."
    │
    ├─ 保存用户消息到 st.session_state.messages
    ├─ 渲染用户气泡
    └─ 调用 st.session_state.agent.chat(prompt)
```

Streamlit 的 `st.session_state.agent` 是一个 `ResearchAgent` 实例，它在用户首次进入或切换会话时创建。

### 第 2 步：Agent 主循环入口 (agent_core.py)

```python
def chat(self, user_message: str) -> Dict:
    trace_id = generate_trace_id()           # 全链路追踪 ID
    set_trace_id(trace_id)                   # 注入 contextvar

    # 2a. 保存用户消息到 MySQL
    self.session_manager.add_message(session_id, "user", user_message)

    # 2b. 构建上下文
    context_messages = self.session_manager.build_context(session_id)
```

**`build_context()` 做了什么？**

```
从 MySQL messages 表读取:
  ┌──────────────────────────────────────────┐
  │ 1. 系统提示 (SYSTEM_PROMPT)               │
  │    "你是一个科研灵感助手 Agent..."          │
  │                                          │
  │ 2. 若有摘要 → 注入                         │
  │    "[以下是之前对话的摘要...]"              │
  │                                          │
  │ 3. 最近 20 条非系统消息                    │
  │    user: "之前的问题..."                   │
  │    assistant: "之前的回答..."              │
  │    user: "有没有使用 wav2vec..."  ← 新消息 │
  └──────────────────────────────────────────┘
```

### 第 3 步：LLM 决策 (agent_core.py + llm_service.py)

```python
response = chat_completion(
    messages=context_messages,    # 上面构建的上下文
    tools=TOOL_DEFINITIONS,       # 2 个工具定义
)
```

发给 Qwen3-max 的请求包含：

```
messages: [system_prompt, ..., user_question]
tools: [
    {name: "search_knowledge_base",  description: "从知识库检索论文片段..."},
    {name: "search_papers_online",   description: "从 arXiv/OpenAlex 联网搜索..."}
]
```

LLM 分析用户意图并决定：

```
用户说 "有没有...论文"、"5篇" → 应该调用 search_papers_online
参数: {query: "wav2vec partial spoofing audio detection", limit: 5}
```

LLM 返回：

```json
{
  "finish_reason": "tool_calls",
  "message": {
    "tool_calls": [{
      "function": {
        "name": "search_papers_online",
        "arguments": {"query": "wav2vec partial spoofing audio detection", "limit": 5}
      }
    }]
  }
}
```

### 第 4 步：路由到 MCP 模块执行 (agent_core.py → paper_search_mcp.py)

```python
if tc["name"] == "search_papers_online":
    executor = mcp_call_tool    # → paper_search_mcp.py
else:
    executor = execute_tool     # → tools.py

result = middleware.execute_with_tracking(
    tool_name=tc["name"],
    tool_args=tc["arguments"],
    executor=executor,
)
```

Middleware 包裹执行：
- `before`: 打印 `🔧 TOOL CALL: search_papers_online | 📥 {query: ..., limit: 5}`
- 执行
- `after`: 打印 `✅ DONE: search_papers_online | ⏱️ 2.1s | 📤 检索到 5 篇论文`

### 第 5 步：联网论文搜索 (paper_search_mcp.py → paper_search.py)

```python
def handle_search_papers(arguments):
    papers = search_papers(query="wav2vec partial...", limit=5)
    papers = translate_abstracts(papers)        # LLM 翻译摘要
    return _mcp_success(llm_text, structured)
```

**`search_papers()` 内部并行检索**：

```
ThreadPoolExecutor(max_workers=3)

Thread-1: search_arxiv(query)           → https://export.arxiv.org/api/query
          双路径: urllib直连 + requests显式代理(127.0.0.1:7897)
          返回: 20 篇 (expanded_limit)

Thread-2: search_openalex(query)        → https://api.openalex.org/works
          返回: 20 篇

Thread-3: search_semantic_scholar(query) → https://api.semanticscholar.org/...
          先检查冷却计时，未冷却则跳过
          返回: 0 或 N 篇

合并: 去重 → 年份过滤 → 按引用量排序 → top-5

摘要翻译: 批量调用 LLM 将英文 abstract 翻译为中文 abstract_cn
```

**每篇论文返回的字段**：

```python
{
    "title": "Are audio DeepFake detection models polyglots?",
    "authors": ["Author A", "Author B", ...],
    "year": 2024,
    "venue": "arXiv",
    "abstract": "The rapid advancement of...",
    "abstract_cn": "深度伪造检测技术的快速发展...",
    "pdf_url": "https://arxiv.org/pdf/2401.xxxxx.pdf",
    "source": "arxiv",
    "citation_count": 12,
    ...
}
```

### 第 6 步：LLM 基于工具结果生成回复 (agent_core.py)

工具结果被追加到 context_messages，发回 LLM：

```python
# 追加 assistant 的 tool_calls 决策
context_messages.append({"role": "assistant", "tool_calls": [...]})

# 追加工具执行结果（简洁文本版，~300字）
context_messages.append({"role": "tool", "content": "检索到 5 篇论文：\n1. ..."})

# 第二次 LLM 调用（不带 tools，让 LLM 生成纯文本回复）
final_response = chat_completion(messages=context_messages, tools=None)
```

LLM 基于论文列表生成回复：

```
"根据检索结果，共有 5 篇相关论文。以下是简要总结分析：

1. **Are audio DeepFake detection models polyglots?** (2024)
   研究深度伪造检测模型在不同语言上的泛化能力...

2. **SafeEar: Content Privacy-Preserving Audio Deepfake Detection** (2024)
   提出了一种隐私保护的音频深度伪造检测方案...

..."
```

### 第 7 步：持久化回复 (agent_core.py → MySQL)

```python
# 把论文元数据编码到消息尾部
persistent_content = encode_papers_in_message(
    final_response,    # LLM 生成的文本
    all_papers         # 论文结构化数据
)
# 结果: "LLM的回复文本...\n\n<!--PAPERS_JSON-->\n[{...}, {...}]\n"

# 写入 MySQL messages 表
self.session_manager.add_message(session_id, "assistant", persistent_content)
```

**为什么要把论文 JSON 塞进消息里？** 为了让论文卡片在重启后还能恢复。`messages` 表只有 `content` 字段（TEXT 类型），论文 JSON 作为隐藏标记附加在末尾，加载时通过 `decode_papers_from_message()` 分离。

### 第 8 步：前端渲染 (app.py)

Agent 返回的 dict：

```python
{
    "response": "根据检索结果，共有 5 篇相关论文...",   # LLM 文本
    "papers": [{...}, {...}, {...}, {...}, {...}],     # 论文结构化数据
    "tool_calls": [...],                               # 工具调用记录
    "retrieved_chunks": [],                            # 知识库检索片段（本次为空）
    "middleware_logs": [...],                          # 工具调用日志
}
```

Streamlit 渲染：

```python
# 1. 渲染 LLM 生成的文本
st.markdown(result["response"])

# 2. 渲染论文卡片（每篇可展开）
for paper in result["papers"]:
    st.expander(f"**{paper['title']}** ({paper['year']})")
        ├─ 作者、引用量
        ├─ 链接: Semantic Scholar / arXiv / PDF
        ├─ 英文摘要
        ├─ 中文摘要（LLM翻译）
        └─ [⬇ 下载 PDF 并加入知识库] 按钮

# 3. 展开面板：工具调用详情
st.expander("🔧 工具调用 (1 次)")
    └─ search_papers_online: {query: ..., limit: 5}

# 4. 保存到 session_state（供 rerun 后渲染）
st.session_state.messages.append({
    "role": "assistant",
    "content": response_text,
    "papers": papers,
})
st.rerun()  # 触发页面重新渲染
```

---

## 四、另一条路径：本地知识库检索

如果用户问的是已有论文的内容（而非找新论文），流程会走 `search_knowledge_base`：

```
用户: "AASIST 论文中使用的损失函数是什么？"
    │
    ▼
LLM 决策: 调用 search_knowledge_base
    │
    ▼
tools.py → _search_knowledge_base(query="AASIST loss function")
    │
    ├─ L1: query_rewriter.py
    │    LLM 将 "AASIST loss function" 拆解为 5 个变体:
    │    "AASIST model objective function"
    │    "AASIST training loss"
    │    "AASIST EER optimization"
    │    "AASIST 损失函数"
    │    原始问题
    │
    ├─ L2: retriever.py → multi_query_search(queries=5变体)
    │    每个变体独立执行:
    │    ┌─────────────────────────────────────────┐
    │    │ BM25 检索 (关键词匹配, jieba分词)         │
    │    │  +                                      │
    │    │ 语义检索 (向量相似度, Chroma)              │
    │    │  → RRF(k=60) 融合 → 每个变体取 top-15    │
    │    └─────────────────────────────────────────┘
    │    5 变体 × 15 = 75 候选 → 去重 → ~50 候选
    │
    └─ L3: reranker.py
         BGE-reranker-large Cross-Encoder
         逐对 (原始问题, chunk_text) 打分
         → top-5 最相关片段
```

### 知识库是怎么建立起来的？

```
用户上传 PDF
    │
    ▼
app.py: MD5 校验 → 查重
    │
    ▼
document_processor.py:
    Docling 将 PDF 转为 Markdown
    │
    ├─ 布局分析: 识别双栏、标题层级
    ├─ OCR: 识别扫描页中的文字
    ├─ 表格识别: 提取表格为 MD 格式
    └─ 输出: 结构化 MD 文本
    │
    ▼
按 ##/### 标题切分为 chunks (max 1000字符, overlap 100)
    │
    ▼
embedding_service.py:
    DashScope text-embedding-v4 → 每个 chunk → 1024维向量
    │
    ▼
双写:
    ├─ MySQL chunks 表: 存 chunk 文本 + 章节名 + 文档ID
    └─ Chroma: 存向量 + 文本 + 元数据
    │
    ▼
retriever.py: 重建 BM25 索引 (内存中)
```

---

## 五、会话持久化 & 自动摘要

### 消息存储

```
每次对话:
  user:     INSERT INTO messages (session_id, role='user', content='...')
  assistant: INSERT INTO messages (session_id, role='assistant', content='...')

论文搜索: content 末尾嵌入 <!--PAPERS_JSON-->{...}
```

### 会话恢复

```
用户切换会话:
  _switch_to_session(session_id)
    → agent.get_session_messages()
      → SELECT * FROM messages WHERE session_id=? ORDER BY created_at
      → 遍历每条消息:
          clean_text, papers = decode_papers_from_message(content)
          → {role, content: clean_text, papers: [...]}
      → 加载到 st.session_state.messages
```

### 自动摘要触发

```
每次 chat() 结束后:
  msg_count = count_messages(session_id)
  if msg_count >= 20:
    ├─ 检查最近摘要覆盖范围 (避免重复摘要)
    ├─ 取全部非系统消息 → LLM 生成四段式摘要:
    │  1. 讨论主题
    │  2. 关键讨论点
    │  3. 涉及论文/知识库
    │  4. 待解决事项
    ├─ 存入 summaries 表
    └─ 后续对话: build_context() 时注入最新摘要到系统提示
```

---

## 六、日志系统

```
                        ┌─────────────────┐
        logger.info() ─→│ ConsoleHandler   │ → stdout 彩色输出 (DEBUG级)
        logger.error()─→│                  │
                        ├─────────────────┤
                        │ RotatingFile    │ → logs/agent.log (INFO级, 30天)
                        │ Handler         │
                        ├─────────────────┤
                        │ ErrorFile       │ → logs/error.log (WARNING+)
                        │ Handler         │
                        ├─────────────────┤
                        │ MemoryRing      │ → deque(500) → Streamlit 日志面板
                        │ Handler         │
                        └─────────────────┘

trace_id (contextvar): 每个请求自动注入，全链路可追踪
```

---

## 七、数据流向总览图

```
                         ┌──────────────────────┐
                         │    Streamlit 前端      │
                         │  (浏览器 ←→ Python)    │
                         └──────┬───────┬───────┘
               用户提问          │       │ 上传PDF/切换会话
        ┌───────────────────────┘       └───────────────┐
        ▼                                               ▼
┌───────────────┐                           ┌──────────────────┐
│  agent_core   │                           │ Docling → chunks │
│  ReAct 循环    │                           │ → MySQL + Chroma │
└───────┬───────┘                           └──────────────────┘
        │
        │ LLM 决策调用哪个 Tool
        │
   ┌────┴────┬──────────────┐
   ▼         ▼              ▼
search_   search_        (未来扩展)
knowl-    papers_
edge_     online
base      (MCP模块)
   │         │
   ▼         ▼
┌──────┐  ┌──────────────────────────┐
│ RAG  │  │ paper_search.py           │
│检索  │  │ 三源并行: arXiv + OpenAlex│
│      │  │          + S2 (冷却后)     │
│BM25  │  │ 双路径HTTP: urllib直连     │
│+语义 │  │           + requests代理   │
│+重排 │  └──────────────────────────┘
└──┬───┘
   │
   ▼
┌──────────────────────┐
│ MySQL ←→ Agent 状态   │
│ sessions             │
│ messages (含papers)   │
│ summaries            │
│ documents            │
│ chunks               │
└──────────────────────┘
```

---

## 八、关键设计决策回顾

| 决策 | 选择 | 原因 |
|------|------|------|
| 前后端通信 | **无 HTTP**，Streamlit 同进程调用 | 项目规模小，无需微服务化 |
| Tool 执行 | **MCP 风格函数调用**，非独立进程 | 学习 MCP 概念，但避免过度工程化 |
| 知识库检索 | BM25 + 语义 + CrossEncoder 三层 | 解决学术术语不统一的核心痛点 |
| 查询改写 | LLM 生成 5 个同义变体 | 同一个概念在不同论文中的表述差异巨大 |
| 论文持久化 | JSON 嵌入消息内容 | 无需改 DB schema，简单可靠 |
| arXiv 连接 | urllib直连 + requests显式代理双路径 | Windows 系统代理不可靠，双路径兜底 |
| S2 限速 | 120s 冷却计时 | 避免连续 429 |
| PDF 解析 | Docling (IBM) | 学术论文专用，支持布局分析+表格+公式 |
| 会话摘要 | 20 条消息触发 | 避免上下文窗口溢出 |
| 日志 | 控制台+文件+内存三通道 | 开发调试 + 持久化 + 前端展示 |

---

## 九、常用调试技巧

### 1. 看日志定位问题

```bash
# 控制台（实时）
streamlit run app.py

# 文件日志
tail -f logs/agent.log     # 所有 INFO+
tail -f logs/error.log     # 仅 ERROR+WARNING

# Streamlit 侧边栏 → 📋 系统日志面板
```

### 2. 追踪单次请求

在日志中搜索 `trace_id`（格式 `req_20260608_103000_a1b2c3`），可还原该请求从 LLM 调用→ Tool 执行→ 结果返回的完整时间线。

### 3. 测试单个模块

```bash
# 测试论文检索
python -c "from knowledge_base.paper_search import search_arxiv; \
  print(len(search_arxiv('test', 3)))"

# 测试查询改写
python -c "from knowledge_base.query_rewriter import rewrite_query; \
  print(rewrite_query('wav2vec partial spoofing'))"

# 测试 PDF 转换
python -c "from knowledge_base.document_processor import process_document; \
  print(process_document('uploads/xxx.pdf'))"
```
