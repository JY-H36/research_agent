# 科研灵感助手 Agent — 需求规格说明书

> **版本**: v1.0  
> **日期**: 2026-06-04  
> **目标用户**: 高校/企业科研人员（以"伪造音频检测"为首要落地场景）  
> **核心定位**: 一个集论文自动发现、知识库管理、综述生成、科研想法评审与方案设计于一体的 AI Agent 系统。

---

## 目录

1. [系统概述与目标](#1-系统概述与目标)
2. [用户角色与权限](#2-用户角色与权限)
3. [功能需求](#3-功能需求)
   - [3.1 论文自动发现与入库](#31-论文自动发现与入库)
   - [3.2 论文智能打标](#32-论文智能打标)
   - [3.3 知识库管理](#33-知识库管理)
   - [3.4 综述文档自动生成](#34-综述文档自动生成)
   - [3.5 科研想法可行性分析](#35-科研想法可行性分析)
   - [3.6 科研方案推荐与生成](#36-科研方案推荐与生成)
   - [3.7 会话管理与持久化](#37-会话管理与持久化)
   - [3.8 上下文自动摘要](#38-上下文自动摘要)
   - [3.9 研究空白与趋势分析](#39-研究空白与趋势分析)
   - [3.10 实验方案辅助设计](#310-实验方案辅助设计)
   - [3.11 多轮深度对话](#311-多轮深度对话)
   - [3.12 多模态支持](#312-多模态支持)
   - [3.13 导出与分享](#313-导出与分享)
4. [标签体系设计](#4-标签体系设计)
5. [知识库设计](#5-知识库设计)
6. [RAG 检索增强生成设计](#6-rag-检索增强生成设计)
7. [Agent 工作流设计](#7-agent-工作流设计)
8. [数据模型概要](#8-数据模型概要)
9. [非功能性需求](#9-非功能性需求)
10. [界面与交互设想](#10-界面与交互设想)
11. [潜在风险与应对](#11-潜在风险与应对)
12. [后续扩展方向](#12-后续扩展方向)
13. [附录：典型使用场景](#13-附录典型使用场景)

---

## 1. 系统概述与目标

### 1.1 项目背景

科研人员在开展研究时面临以下痛点：

- **信息过载**：每年数以万计的论文发表，手动检索和筛选耗时巨大。
- **知识碎片化**：读过的论文容易遗忘，缺乏结构化的知识组织方式。
- **创新困难**：难以系统性地判断一个想法是否已被发表、是否可行、与现有工作的差异在哪里。
- **写作负担重**：撰写综述（Related Work / Survey）需要大量时间整理和归纳。
- **方案设计缺乏参考**：有了想法之后，如何设计实验方案、选择基线、构建数据集往往无从下手。

### 1.2 产品目标

构建一个面向科研人员的 AI Agent，以"研究方向"为锚点，提供从**文献发现 → 知识组织 → 想法验证 → 方案设计**的全流程辅助。

### 1.3 核心能力一句话总结

> 告诉 Agent 你的研究方向，它帮你找论文、建知识库、写综述、评审你的科研想法并给出可行的实验方案。

### 1.4 首选落地场景

以**伪造音频检测（Audio Deepfake Detection）**为首要验证场景，包括但不限于：

- AI 合成语音检测（TTS / VC 生成语音）
- 音频篡改检测（裁剪、拼接、插入、删除）
- 部分伪造检测（Partial Fake Detection）
- 跨数据集泛化能力评估
- 对抗攻击鲁棒性研究

系统设计应具备**领域无关性**，通过配置可迁移到任何研究方向（计算机视觉、自然语言处理、机器人、生物信息学等）。

---

## 2. 用户角色与权限

| 角色 | 权限描述 |
|------|----------|
| **普通用户（研究者）** | 创建/管理研究方向、使用全部科研辅助功能、管理个人知识库、查看个人会话历史 |
| **管理员** | 用户管理、系统配置、知识库全局管理、查看使用统计、API配额管理 |
| **访客（未来扩展）** | 浏览公开的综述文档、查看 demo 示例 |

> 首期仅实现**普通用户**角色，单用户模式（每个研究者拥有独立的知识库和会话空间）。

---

## 3. 功能需求

### 3.1 论文自动发现与入库

#### 3.1.1 输入
- 用户指定**研究方向**（如 "audio deepfake detection"、"伪造音频检测"）
- 可选：限定时间范围（如 2020-2026）
- 可选：指定会议/期刊（如 ICASSP、Interspeech、TASLP）
- 可选：关键词组合（中英文均支持）

#### 3.1.2 功能描述
Agent 自动完成以下流程：

1. **搜索阶段**
   - 调用 Google Scholar（或 Semantic Scholar / arXiv / DBLP API 作为备选）进行检索
   - 默认返回 3~5 篇高相关度论文（用户可配置数量 3~20 篇）
   - 按引用量、发表时间、相关性综合排序
   - 支持分页与"加载更多"

2. **信息提取阶段**
   - 自动提取每篇论文的：
     - 标题（中/英）
     - 作者列表
     - 发表年份
     - 会议/期刊名称
     - 摘要（Abstract）
     - 关键词
     - DOI / arXiv ID
     - 被引次数
     - PDF 下载链接（若有）
   - 尝试获取论文全文（优先 arXiv / 开放获取），若无法获取则仅保留元数据

3. **深度分析阶段**（对获取到全文的论文）
   - 提取论文的核心方法/模型名称
   - 提取使用的数据集名称
   - 提取主要评价指标与数值
   - 识别对比的基线方法
   - 提取核心贡献点
   - 识别局限性（limitations / future work 章节）

4. **入库阶段**
   - 自动存入知识库（见 [3.3 知识库管理](#33-知识库管理)）
   - 自动打标签（见 [3.2 论文智能打标](#32-论文智能打标)）
   - 去重检测：基于 DOI / arXiv ID / 标题相似度，避免重复入库
   - 入库后通知用户，展示新增论文概览

#### 3.1.3 爬取策略与合规
- 遵守 robots.txt 和网站使用条款
- 设置合理的请求频率（如每 5~10 秒一次请求）
- 优先使用官方 API（Semantic Scholar API 免费且无速率限制）
- 缓存已爬取结果，避免短时间内重复请求
- 明确告知用户爬取来源，尊重版权（仅用于个人研究）

---

### 3.2 论文智能打标

#### 3.2.1 标签维度

每篇论文入库后，Agent 自动从以下维度打标签（可多选）：

| 标签维度 | 示例值 | 说明 |
|----------|--------|------|
| **研究领域** | 伪造音频检测、语音合成、说话人识别、计算机视觉、NLP | 一级领域分类 |
| **研究对象** | 音频、图像、视频、文本、多模态、传感器数据 | 数据类型 |
| **任务类型** | 二分类、多分类、回归、异常检测、生成、检索 | 任务形式 |
| **方法范式** | 深度学习、传统机器学习、信号处理、规则方法、混合方法 | 方法论层面 |
| **模型架构** | CNN、RNN/LSTM、Transformer、GAN、Diffusion、RNN+CNN混合、wav2vec、HuBERT、RawNet2、AASIST | 具体架构 |
| **优化内容** | 模型架构、损失函数、数据增强、训练策略、特征工程、后处理 | 优化的目标模块 |
| **贡献类别** | 算法优化、构建数据集、理论分析、基准评测、综述、复现研究 | 论文贡献类型 |
| **数据集** | ASVspoof2019、ASVspoof2021、FoR、WaveFake、LJSpeech、自制数据集 | 使用/提出哪些数据集 |
| **特征类型** | 原始波形、频谱图、MFCC、CQCC、LFCC、嵌入向量、多特征融合 | 输入特征 |
| **评价指标** | EER、t-DCF、Accuracy、F1、AUC、ROC | 核心指标 |
| **发表年份** | 2020、2021、2022、2023、2024、2025 | 时间维度 |
| **发表来源** | ICASSP、Interspeech、TASLP、SPL、NeurIPS、ICML、CVPR、arXiv | 学术来源 |
| **语种** | 中文、英文、多语种 | 论文语言 |
| **开源状态** | 有代码、无代码、部分开源 | 可复现性 |
| **创新程度** | 突破性、渐进式改进、应用迁移、综述 | 创新层次 |

#### 3.2.2 打标方式
- **首次入库**：Agent 基于论文标题、摘要、全文进行自动打标，给出置信度
- **用户修正**：用户可以增删改标签，修正结果会作为反馈优化后续打标准确率
- **批量打标**：支持对已有论文重新触发打标（如标签体系更新后）
- **标签推荐**：用户浏览论文时，Agent 可推荐相关标签供确认

#### 3.2.3 标签用途
- 知识库筛选与过滤
- 综述文档的自动分类与组织
- RAG 检索时提高召回精度
- 研究趋势统计（如"2024年Transformer类方法占比"）

---

### 3.3 知识库管理

#### 3.3.1 论文入库
- 支持来源：
  - Agent 自动爬取（Google Scholar / Semantic Scholar / arXiv）
  - 用户手动上传 PDF 文件（单个/批量）
  - 用户手动输入 URL（DOI / arXiv 链接）
  - 用户粘贴 BibTeX 条目
  - 从 Zotero / Mendeley 导出文件导入（未来扩展）

#### 3.3.2 MD5 去重机制
- 上传/入库前，对文件计算 MD5 哈希值
- 与知识库中已有文件的 MD5 进行比对
- 若 MD5 匹配 → 拒绝入库，提示用户"该文献已存在于知识库中"，并展示已有条目
- MD5 不匹配但标题高度相似（相似度 > 90%）→ 提示用户确认是否重复
- 去重范围：同一用户的知识库内；可选择是否跨用户去重

#### 3.3.3 知识库组织
- **按研究方向分库**：一个用户可拥有多个研究方向的知识库（如"伪造音频检测"、"语音增强"），彼此隔离也可交叉检索
- **按标签分类浏览**：树状/网状标签导航
- **按时间线浏览**：以发表年份为轴的论文时间线
- **收藏/星标**：标记重要论文
- **阅读状态**：未读 / 已读 / 精读 / 已归档
- **个人笔记**：对每篇论文添加私人笔记（Markdown 格式）
- **关联关系**：手动/自动建立论文之间的引用关系、对比关系、扩展关系

#### 3.3.4 知识库操作
- **搜索**：全文搜索、标签搜索、作者搜索、时间范围搜索、组合搜索
- **筛选**：按任意标签维度筛选
- **排序**：按引用量、发表时间、入库时间、相关性排序
- **导出**：导出为 BibTeX / CSV / JSON 格式
- **删除**：软删除（回收站）+ 永久删除
- **批量操作**：批量打标、批量导出、批量删除

#### 3.3.5 知识库统计
- 论文总数、各标签论文数量分布
- 论文发表年份分布直方图
- 作者/机构合作网络（未来扩展）
- 引用关系图谱（未来扩展）

---

### 3.4 综述文档自动生成

#### 3.4.1 生成触发
- 用户指定研究方向 + 可选标签筛选条件（如"只综述 Transformer 类方法"）
- 用户选择综述的组织逻辑（见下方）

#### 3.4.2 组织逻辑（用户可选）

| 组织方式 | 适用场景 | 说明 |
|----------|----------|------|
| **模型演进顺序** | 方法快速迭代的领域 | 按时间线梳理模型演变路径，如 RawNet2 → AASIST → RawGAT-ST |
| **技术路线分类** | 多种技术路线并存 | 按方法范式分类，如"基于手工特征的方法"、"基于端到端深度学习的方法"、"基于自监督学习的方法" |
| **问题驱动** | 领域有明确的子问题 | 按解决的子问题组织，如"全伪造检测" vs "部分伪造检测"、"已知攻击检测" vs "未知攻击泛化" |
| **数据集/评测基准** | 评测驱动的研究 | 以主流数据集为章节，每个数据集下介绍代表性方法及性能 |
| **混合逻辑** | 综合场景 | 先按技术路线分大类，每类内部按时间演进 |

#### 3.4.3 综述内容结构

每篇生成的综述文档应包含：

1. **摘要**：200~300 字概括研究背景、主流方法、现状与挑战
2. **引言**：问题定义、研究意义、综述范围与组织方式说明
3. **背景知识**：必要的基础概念解释（如"什么是 Equal Error Rate (EER)"）
4. **方法分类与详述**（主体）：
   - 每一类方法包含：
     - 核心思想
     - 代表性工作（2~4 篇），每篇说明：方法名、核心创新、使用的数据集、关键性能指标
     - 优缺点分析
   - 方法之间演进关系说明（如"A 方法解决了 B 方法的什么问题"）
5. **数据集与评价指标汇总**：表格汇总所有提及的数据集和指标
6. **性能对比表**：代表性方法的性能横向对比（数据集 × 方法 × 指标）
7. **开放挑战与未来方向**：从论文的 limitation/future work 中归纳，结合 Agent 的分析
8. **参考文献**：自动生成 BibTeX 格式的参考文献列表

#### 3.4.4 综述质量保证
- 所有论述必须基于知识库中的真实论文，不可编造
- 每一条关键论断需标注引用来源（引用到具体论文）
- 性能数据必须从论文中提取，不可推断
- 用户可对生成的综述进行编辑和批注
- 支持增量更新：知识库新增论文后，可选择"更新综述"而非重新生成

#### 3.4.5 输出格式
- Markdown 格式（默认，支持 LaTeX 数学公式）
- 可选导出为 PDF（LaTeX 渲染）、HTML、Word
- 图表自动生成（演进时间线图、性能对比柱状图等）

---

### 3.5 科研想法可行性分析

#### 3.5.1 输入
用户用自然语言描述科研想法，例如：

> "我想用对比学习（Contrastive Learning）来做伪造音频检测，让模型学习真实音频和伪造音频在嵌入空间中的差异表征。"

#### 3.5.2 处理流程

```
用户想法 → RAG 检索相关知识 → 多维度分析 → 可行性判定 + 方案建议
```

**Step 1 — RAG 检索**
- 将用户想法向量化，在知识库中进行语义检索
- 同时进行标签匹配检索（如识别出"对比学习"→ 检索相关标签）
- 检索范围不限于用户自身知识库，可扩展到外部公开论文库（需联网）
- 返回 Top-K（K 可配置，默认 10）最相关论文/知识片段

**Step 2 — 多维度分析**

| 分析维度 | 分析内容 |
|----------|----------|
| **新颖性检查** | 用户想法是否已被已发表的论文覆盖？若有高度相似的工作，列出并对比差异 |
| **技术可行性** | 从技术实现角度分析是否可行：有可用的开源代码吗？数据集是否公开？计算资源需求如何？ |
| **理论合理性** | 方法在理论上是否站得住脚？对比学习的假设在音频伪造检测场景下是否成立？ |
| **与现有工作的关系** | 用户的想法的定位是什么？是全新方向 / 改进现有方法 / 跨领域迁移？ |
| **数据集与评测** | 建议使用哪些数据集？需要构建新数据集吗？建议的评价指标是什么？ |
| **潜在难点与风险** | 正样本/负样本如何定义？数据增强策略？可能的过拟合风险？跨数据集泛化挑战？ |
| **创新空间评估** | 这个方向上还有多少可做的空间？是否已是红海？有哪些未被充分探索的角度？ |

**Step 3 — 判定输出**
- **可行性结论**：高可行 / 可行（需注意XX）/ 低可行（原因）/ 已被发表
- **详细分析报告**：包含各维度的分析结果
- **相关论文展示**：RAG 检索到的关键论文及其与用户想法的关联
- **差异化建议**：如果该方向已有工作，建议如何做出差异化

#### 3.5.3 交互模式
- **一次性分析**：用户提交想法 → Agent 返回完整分析报告
- **对话式深挖**：Agent 先给出初步分析，用户追问具体某个方面（如"数据增强策略具体怎么设计？"），Agent 在对话上下文中进一步深入

---

### 3.6 科研方案推荐与生成

#### 3.6.1 功能描述
当用户的科研想法被判定为"可行"后，Agent 进一步生成具体的科研实施方案。

#### 3.6.2 方案内容

1. **问题形式化定义**
   - 输入/输出定义
   - 数学形式化描述
   - 评估协议（Evaluation Protocol）

2. **方法设计**
   - 模型架构建议（可给出 2~3 个备选方案，分析各自优劣）
   - 损失函数设计建议
   - 训练策略建议（学习率调度、优化器选择、batch size 等）
   - 数据增强策略
   - 特征选择建议

3. **实验设计**
   - 数据集选择（主实验 + 泛化实验）
   - 基线方法列表（需包含经典方法和最新 SOTA）
   - 评价指标
   - 消融实验设计（验证各模块的贡献）
   - 统计显著性检验方案

4. **预期结果与贡献总结**
   - 预期提升幅度（基于文献中类似改进的参考值）
   - 论文可能的贡献点
   - 建议的论文标题（3~5 个备选）

5. **实施路线图**
   - Phase 1：基线复现与验证（预估时间）
   - Phase 2：核心方法实现与调优
   - Phase 3：全面实验与消融研究
   - Phase 4：论文撰写

#### 3.6.3 方案定制化
- **资源约束输入**：用户可告知 GPU 资源限制（如"只有一张 RTX 3090"），方案会相应调整模型规模建议
- **时间约束输入**：如"想在 3 个月内投 Interspeech"，方案会评估可行性并调整范围
- **目标会议/期刊输入**：根据目标会议偏好调整方案风格（如 Interspeech 偏工程、TASLP 偏深度）

---

### 3.7 会话管理与持久化

#### 3.7.1 会话定义
一个会话（Session）是用户与 Agent 围绕某个主题的连续对话。例如：
- "关于伪造音频检测综述的讨论"
- "关于对比学习方案的深度讨论"

#### 3.7.2 MySQL 持久化内容

| 持久化对象 | 主要内容 |
|-----------|----------|
| **用户信息** | 用户 ID、用户名、偏好设置、研究方向列表 |
| **会话元数据** | 会话 ID、标题、创建时间、最后活跃时间、关联的研究方向、消息数量、摘要状态 |
| **会话消息** | 消息 ID、角色（用户/Agent/系统）、内容（完整文本）、token 数、时间戳、消息类型（普通/系统通知/分析报告） |
| **知识库条目** | 论文元数据、标签、MD5 哈希、入库时间、阅读状态、用户笔记、全文存储路径 |
| **标签体系** | 标签维度定义、标签值、标签层级关系 |
| **综述文档** | 综述 ID、关联研究方向、生成时间、内容、组织逻辑、关联论文列表 |
| **分析报告** | 报告 ID、关联想法、分析结果、生成时间 |
| **方案文档** | 方案 ID、关联想法、方案内容、生成时间 |
| **用户反馈** | 对检索结果、综述、分析、方案的评分和反馈文本 |

#### 3.7.3 会话恢复
- 用户可查看历史会话列表（按时间、研究方向筛选）
- 点击任意历史会话可恢复对话上下文
- 会话列表展示：标题、研究方向、消息数量、创建时间、最后活跃时间

#### 3.7.4 数据库设计原则
- 使用 InnoDB 引擎，支持事务
- 对高频查询字段建立索引（用户 ID、会话 ID、时间戳、标签）
- 大文本字段（论文全文、综述内容）使用 LONGTEXT 类型或考虑文件系统存储 + 数据库存路径
- 向量嵌入存储（用于 RAG 检索）可考虑独立的向量数据库（如 Milvus、Qdrant、Chroma）
- 定期备份策略

---

### 3.8 上下文自动摘要

#### 3.8.1 触发条件
- **Token 阈值触发**：当会话累计 token 数超过预设阈值（如 8000 tokens，可配置），自动触发摘要
- **轮次阈值触发**：当对话轮次超过预设阈值（如 20 轮）
- **会话切换触发**：用户切换到其他会话时，对当前会话进行摘要
- **手动触发**：用户随时可手动触发"总结当前会话"

#### 3.8.2 摘要内容
生成的摘要应包含：
1. **会话主题**：一句话概括讨论的核心主题
2. **讨论脉络**：按时间线梳理讨论的关键节点
3. **关键结论**：对话中达成的重要结论或决策
4. **待办事项**：来讨论中提到但尚未解决的事项
5. **相关论文索引**：对话中涉及的论文列表
6. **生成的产出物**：会话中生成的综述、分析报告、方案等

#### 3.8.3 摘要策略
- **渐进式摘要**（推荐）：不是一次性压缩全部历史，而是维护多层摘要
  - **近期对话**（最近 N 轮）：保留完整原文
  - **中期对话**：生成详细摘要（保留关键细节）
  - **远期对话**：生成精炼摘要（仅保留核心结论）
- **摘要注入**：生成摘要后，将其作为系统提示的一部分注入后续对话中，确保 Agent 保持对整体语境的感知
- **摘要链**：多次摘要之间建立关联（摘要的摘要），确保跨长对话的知识连续性

#### 3.8.4 摘要存储
- 摘要存储在数据库中，关联到原会话
- 用户可查看、编辑、删除摘要
- 摘要可用于会话列表预览（在列表中展示会话摘要而非首条消息）

---

### 3.9 研究空白与趋势分析

#### 3.9.1 研究空白发现
基于知识库中已收集的论文，Agent 分析并提出可能的研究空白：

- **方法层面空白**：哪些有效的方法尚未在该领域被尝试？（如"Graph Neural Network 在伪造音频检测中应用较少"）
- **问题层面空白**：哪些子问题尚未被充分研究？（如"低资源语言的伪造音频检测"）
- **数据集层面空白**：现有数据集的局限性？（如"缺乏真实场景下的部分伪造数据"）
- **评估层面空白**：现有评估协议的不足？（如"缺乏对对抗攻击鲁棒性的系统评估"）

#### 3.9.2 研究趋势分析
- **方法趋势**：近 3 年各类方法的论文数量变化趋势（柱状图/折线图）
- **热点转移**：哪些方向在兴起（如"自监督学习在伪造检测中的应用快速增长"）
- **关键节点**：识别领域发展的关键转折点论文（高被引 + 方法范式转变）
- **机构/作者趋势**：高产机构与作者、新兴研究力量
- **会议/期刊偏好**：该领域论文主要发表在哪些会议

#### 3.9.3 周期性报告
- 用户可设置周期性（每周/每月）自动扫描最新论文
- 生成"领域动态简报"，包含：新增论文概览、趋势变化、值得关注的新方向

---

### 3.10 实验方案辅助设计

#### 3.10.1 基线选择推荐
- 根据研究方向自动列出经典基线 + 最新 SOTA
- 标注基线的特点（如"AASIST：当前 SOTA，有开源代码，基于图注意力网络"）
- 给出每个基线的推荐优先级和实现难度

#### 3.10.2 数据集推荐
- 列出领域内常用公开数据集
- 标注数据集的规模、语种、标注方式、获取方式
- 评估数据集的局限性（如"ASVspoof2019 以英语为主，中文数据缺乏"）
- 如需构建新数据集，给出数据采集和标注方案建议

#### 3.10.3 评价协议设计
- 推荐标准评价指标及其适用场景
- 推荐数据集划分方式（train/dev/test split 策略）
- 建议的统计显著性检验方法
- 建议的可视化方式（ROC 曲线、混淆矩阵、t-SNE 可视化等）

#### 3.10.4 计算资源估算
- 基于模型规模和数据集大小估算训练时间
- 建议的硬件配置（GPU 型号、内存、存储）
- 预估实验周期的整体时间线

---

### 3.11 多轮深度对话

#### 3.11.1 上下文保持
- Agent 需在会话中保持对以下内容的持续感知：
  - 用户的研究方向
  - 当前讨论的核心问题
  - 已检索/引用过的论文
  - 已生成的结论和方案
  - 用户的偏好和约束

#### 3.11.2 对话引导
- 当用户输入比较模糊时，Agent 主动提出澄清问题
- 当分析到关键决策点时，Agent 给出 2~4 个选项供用户选择
- 对话方向发生偏移时，Agent 温和地引导回主线

#### 3.11.3 多会话并行
- 用户可以同时拥有多个活跃会话（如不同研究方向或不同子课题）
- 会话之间可共享知识库，但对话上下文独立
- 支持跨会话引用（如"把我上次关于数据增强的讨论结果拿过来"）

---

### 3.12 多模态支持

#### 3.12.1 论文中的图表理解
- PDF 中的图表（模型架构图、实验结果表、流程图）应被识别和理解
- Agent 能够描述图表内容并将关键信息纳入知识库

#### 3.12.2 音频/图像/视频分析（领域相关）
- 伪造音频检测场景：支持用户上传音频样本，Agent 尝试分析特征（如"这段音频的频谱显示出典型的 TTS 伪影"）
- 通用场景：支持用户上传图片（如手绘模型架构草图），Agent 理解并转化为文字描述

#### 3.12.3 公式理解与生成
- 支持 LaTeX 数学公式的理解与生成
- 对论文中的核心公式进行提取和解释

---

### 3.13 导出与分享

- **综述导出**：PDF（LaTeX 渲染）、Markdown、HTML、Word
- **方案导出**：Markdown + 甘特图（实验时间线）
- **知识库导出**：BibTeX、CSV、JSON、Zotero 兼容格式
- **分享链接**：生成只读分享链接，供合作者查看综述或方案
- **协作模式**（未来扩展）：多人共享知识库，协作文档编辑

---

## 4. 标签体系设计

### 4.1 标签层级结构

标签按维度组织为树状结构，支持：
- **一级维度**（必须）：研究领域、研究对象、任务类型
- **二级维度**（推荐）：方法范式、模型架构、贡献类别
- **三级维度**（可选）：数据集、特征类型、评价指标

### 4.2 标签扩展性
- 管理员可添加新的标签维度和标签值
- 用户可自定义私有标签（不影响系统推荐标签）
- 标签值支持别名（如 "ASVspoof2019 LA" 与 "ASVspoof 2019 Logical Access" 视为同一标签）

### 4.3 标签权重
- 每篇论文的每个标签可以有**主标签/次标签**之分
- 如一篇论文的核心贡献是构建数据集，次要贡献是提出新的模型，则"构建数据集"为主标签、"算法优化"为次标签

---

## 5. 知识库设计

### 5.1 知识存储分层

```
┌────────────────────────────────────┐
│         用户可见层                   │
│  论文元数据 / 标签 / 笔记 / 综述      │
├────────────────────────────────────┤
│         向量嵌入层                   │
│  Chunk 文本 → Embedding → 向量DB     │
├────────────────────────────────────┤
│         原始文件层                   │
│  PDF / HTML / 图片（文件系统存储）     │
├────────────────────────────────────┤
│         索引层                       │
│  全文搜索索引 / 标签索引 / 关系索引    │
└────────────────────────────────────┘
```

### 5.2 文档分块策略（Chunking）
- **按章节分块**：基于论文结构（Abstract / Introduction / Method / Experiment / Conclusion）
- **语义分块**：基于文本语义边界自动分割
- **固定大小分块**：每 512/1024 tokens 一块，带 overlap（128 tokens）
- **混合策略**：优先按章节+语义分块，超长章节回退到固定大小分块
- 每个 chunk 保留元数据（来源论文、章节、页码）

### 5.3 向量化
- 使用 Embedding 模型（如 text-embedding-3-large 或 bge-large-zh）将 chunk 向量化
- 向量维度：根据所选 Embedding 模型确定（如 1024 / 1536 / 3072）
- 支持中文+英文混合嵌入
- 定期重新索引（知识库有大量更新后）

### 5.4 检索策略
- **混合检索**：语义检索（向量相似度）+ 关键词检索（BM25）+ 标签过滤
- **重排序（Re-ranking）**：初检结果用 Cross-Encoder 模型重排序，提高精准度
- **检索结果多样化**：避免返回高度相似的 chunk，保证信息覆盖广度

---

## 6. RAG 检索增强生成设计

### 6.1 检索流程

```
用户输入 → Query 改写/扩展 → 多路召回 → 融合排序 → Top-K 结果 → 注入 LLM Prompt
```

### 6.2 Query 改写与扩展
- **Query 扩展**：将用户的简短查询扩展为更完整的检索查询
  - 例："对比学习伪造检测" → "contrastive learning for audio deepfake detection; self-supervised representation learning for synthetic speech detection"
- **中英文互译扩展**：中文查询同时检索英文论文，反之亦然
- **同义词扩展**：基于领域词典自动扩展同义词（如 "deepfake" = "synthetic" = "spoof" = "AI-generated"）
- **子问题分解**：复杂查询拆分为多个子查询分别检索

### 6.3 结果融入策略
- **上下文窗口管理**：根据 LLM 上下文窗口大小，动态分配检索结果占比
- **分层融入**：最相关的结果完整展示，次相关的结果以摘要形式展示
- **引用标注**：每个检索结果附带来源信息，确保可追溯性
- **检索不足处理**：如果检索结果相关度普遍偏低，Agent 应诚实告知而非编造，并建议用户调整查询或扩充知识库

### 6.4 检索效果评估
- 记录每次检索的召回结果和相关度评分
- 用户可对检索结果进行"有用/无用"反馈
- 基于反馈持续优化检索策略

---

## 7. Agent 工作流设计

### 7.1 整体架构模式

采用 **Plan-then-Execute** 模式：

```
用户输入 → 意图识别 → 任务规划 → 逐步执行（调用工具） → 结果整合 → 输出
```

### 7.2 核心工具（Tools）定义

| 工具名称 | 功能 | 输入 | 输出 |
|----------|------|------|------|
| `search_papers` | 从学术搜索引擎检索论文 | 查询词、数量、时间范围、来源过滤 | 论文元数据列表 |
| `fetch_paper_fulltext` | 获取论文全文内容 | DOI/URL/arXiv ID | 全文文本（结构化提取） |
| `add_to_knowledge_base` | 论文入库+打标 | 论文元数据+全文 | 入库确认+标签列表 |
| `search_knowledge_base` | RAG 检索知识库 | 查询文本、Top-K、标签过滤 | 相关 chunk + 来源元数据 |
| `generate_survey` | 生成综述文档 | 研究方向、组织逻辑、范围过滤 | 综述 Markdown |
| `analyze_idea` | 分析科研想法可行性 | 科研想法文本 | 可行性分析报告 |
| `generate_proposal` | 生成科研方案 | 科研想法+约束条件 | 完整方案文档 |
| `summarize_session` | 生成会话摘要 | 会话 ID | 摘要文本 |
| `manage_tags` | 管理论文标签 | 论文 ID、操作类型（增/删/改） | 操作结果 |
| `upload_document` | 上传文档到知识库 | PDF 文件 | MD5 校验结果 + 入库结果 |
| `search_trends` | 研究趋势分析 | 研究方向、时间范围 | 趋势分析报告 |

### 7.3 工作流示例

#### 工作流 1：新手上路
```
用户: "我的研究方向是伪造音频检测"
→ Agent: 确认研究方向 → search_papers(3-5篇) → 展示论文概览 → 询问是否入库
→ 用户确认 → add_to_knowledge_base → 自动打标 → 展示知识库状态
→ Agent: "是否要我基于这些论文生成一份综述？"
```

#### 工作流 2：想法评审
```
用户: "我想把对比学习用在伪造音频检测上"
→ Agent: search_knowledge_base("contrastive learning audio deepfake detection")
→ analyze_idea("对比学习 + 伪造音频检测", retrieved_papers)
→ 展示可行性分析（新颖性、可行性、与现有工作的关系）
→ 用户追问 → 多轮对话深挖
→ 用户: "帮我生成一份详细方案" → generate_proposal(...)
```

#### 工作流 3：周期性更新
```
定时触发 → search_papers(最近一周/月的新论文) → 去重 → 通知用户
→ 用户可选择性入库 → 若入库 → 询问是否更新综述
```

---

## 8. 数据模型概要

### 8.1 核心实体关系

```
User (1) ──── (N) Research_Direction
User (1) ──── (N) Session
User (1) ──── (N) Knowledge_Base

Research_Direction (1) ──── (N) Paper
Research_Direction (1) ──── (N) Survey

Session (1) ──── (N) Message
Session (1) ──── (0..1) Summary

Paper (N) ──── (N) Tag
Paper (1) ──── (N) Paper_Chunk
Paper (N) ──── (N) Paper (引用关系)

Survey (1) ──── (N) Paper (引用)
```

### 8.2 关键表结构（逻辑层面）

**users**
- id, username, email, preferences(JSON), created_at, updated_at

**research_directions**
- id, user_id, name, description, keywords, created_at

**papers**
- id, user_id, title, authors, year, venue, abstract, doi, arxiv_id, md5_hash, full_text_path, citation_count, reading_status, notes, created_at

**paper_chunks**
- id, paper_id, chunk_index, content, embedding(vector), section_name, page_number

**tags**
- id, dimension, value, parent_tag_id

**paper_tags**
- paper_id, tag_id, is_primary, confidence

**sessions**
- id, user_id, research_direction_id, title, status, message_count, token_count, created_at, last_active_at

**messages**
- id, session_id, role, content, token_count, message_type, created_at

**session_summaries**
- id, session_id, summary_level, content, token_range, created_at

**surveys**
- id, user_id, research_direction_id, title, organization_logic, content, paper_ids(JSON), created_at, updated_at

**analysis_reports**
- id, session_id, idea_text, analysis_result(JSON), related_paper_ids(JSON), created_at

**proposals**
- id, session_id, analysis_report_id, proposal_content(JSON), created_at

---

## 9. 非功能性需求

### 9.1 性能要求
- **论文检索**：搜索 + 信息提取应在 30 秒内完成（3~5 篇）
- **综述生成**：10 篇论文规模的综述应在 5 分钟内生成
- **RAG 检索**：向量检索延迟 < 2 秒
- **对话响应**：一般对话回复延迟 < 5 秒（不含大型文档生成）
- **并发支持**：首期支持单用户，未来支持 10+ 并发用户

### 9.2 可用性要求
- **自然语言交互**：用户全程使用自然语言（中文/英文），无需学习特殊语法
- **渐进式呈现**：长篇内容（综述/方案）以流式方式逐步展示，避免长时间等待
- **错误处理**：检索失败、分析无法完成时，Agent 应给出明确说明和建议
- **引导式上手**：新用户首次使用时，Agent 主动引导完成第一步操作

### 9.3 可靠性要求
- **论文信息准确性**：自动提取的论文信息应保持高准确率，关键字段（标题、作者、年份）准确率应接近 100%
- **去重可靠性**：MD5 + 标题相似度双重校验，误判率应极低
- **数据持久性**：MySQL 数据应有定期备份策略
- **引述真实性**：综述和分析报告中的引用必须与知识库中论文对应，不允许幻觉

### 9.4 安全性要求
- **用户数据隔离**：不同用户的知识库和会话严格隔离
- **API 密钥管理**：LLM API 密钥等敏感信息使用环境变量或秘钥管理服务
- **输入过滤**：防止 SQL 注入、XSS 等常见攻击
- **爬取合规**：遵守目标网站的爬取规则和使用条款
- **版权尊重**：下载的论文 PDF 仅用于用户个人研究，系统不公开分发

### 9.5 可扩展性要求
- **领域无关性**：标签体系、知识库结构不硬编码特定研究领域
- **LLM 可替换**：支持 OpenAI、Anthropic、开源模型（Llama、Qwen 等）的灵活切换
- **Embedding 模型可替换**：向量化模型可配置
- **搜索引擎可扩展**：支持接入新的学术搜索数据源
- **多语言**：支持中英文混合检索和分析

### 9.6 可维护性要求
- **日志系统**：关键操作（检索、入库、生成）需有详细日志
- **使用统计**：记录用户使用频次、常用功能、知识库增长趋势
- **反馈闭环**：用户对 Agent 输出的反馈应被记录并用于改进

---

## 10. 界面与交互设想

### 10.1 整体布局
采用 Web 应用的三栏式布局：

```
┌──────────┬──────────────────────────────┬─────────────┐
│          │                              │             │
│  会话     │        对话主区域              │  知识库      │
│  列表     │                              │  浏览器      │
│          │  - 对话消息                    │             │
│  - 会话1  │  - 综述/分析/方案              │  - 论文列表   │
│  - 会话2  │  - 输入框                     │  - 标签过滤   │
│  - 新建   │                              │  - 搜索      │
│          │                              │             │
└──────────┴──────────────────────────────┴─────────────┘
```

### 10.2 核心交互方式
- **对话式交互**（主要）：类 ChatGPT 的对话界面，支持 Markdown 渲染、代码高亮、公式渲染
- **文档预览**（辅助）：侧边栏展开预览综述、方案等长文档
- **知识库面板**（辅助）：可收起/展开的知识库浏览面板

### 10.3 特殊交互元素
- **论文卡片**：在对话中展示论文时，以卡片形式呈现（标题、作者、年份、标签、关键指标），可点击展开详情
- **分析报告**：结构化展示可行性分析各维度，使用折叠面板
- **方案预览**：方案以结构化 Markdown 呈现，支持代码块语法高亮
- **进度指示**：长时间任务（综述生成、爬取）显示进度条或步骤指示器
- **引用悬浮**：鼠标悬停在引用标记上时，显示论文标题+作者+年份

---

## 11. 潜在风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| **Google Scholar 反爬** | 论文自动发现功能失效 | 优先使用 Semantic Scholar API（免费+稳定）；多数据源互为备份（arXiv API、DBLP）；设置合理爬取频率 |
| **LLM 幻觉（编造论文）** | 综述/分析中出现不存在的论文或数据 | 强制所有引用必须来自知识库；引用验证步骤；在 Prompt 中强调"不要编造"；输出后自动校验引用是否存在 |
| **知识库膨胀** | 随着论文增多，检索质量下降 | 渐进式索引优化；定期清理低质量条目；支持知识库归档 |
| **用户期望过高** | 用户期望 Agent 能完全替代人工科研判断 | 在交互中明确 Agent 定位为"辅助工具"；分析结论附带不确定性说明；关键决策建议用户自行验证 |
| **隐私泄露** | 用户上传未发表论文或敏感数据 | 明确隐私政策；数据加密存储；用户控制数据删除权；不上传数据至第三方 |
| **API 费用过高** | 频繁调用 LLM API 造成高昂费用 | 合理的缓存策略（相同查询不重复调用）；支持本地部署模型；分级使用（简单任务用小模型）；使用额度预警 |

---

## 12. 后续扩展方向

### 12.1 短期（v1.1 — v1.5）
- 支持更多学术数据源（PubMed、IEEE Xplore、中国知网等）
- 论文引用关系图谱可视化
- 实验代码模板生成（基于论文的开源代码自动生成 baseline 代码骨架）
- 邮件/微信通知（新论文提醒、周期性报告推送）
- 与 Zotero / Mendeley 的双向同步
- 移动端适配（至少支持移动端查看）

### 12.2 中期（v2.0）
- **多人协作**：研究小组共享知识库、协同编辑综述
- **审稿辅助**：模拟审稿人评审用户论文草稿，指出潜在问题
- **代码辅助**：根据论文方法描述，尝试生成核心代码实现（PyTorch/TensorFlow）
- **实验跟踪**：与 MLflow / wandb 集成，跟踪实验状态和结果
- **研究课题推荐**：基于用户知识库和研究兴趣，主动推荐值得探索的课题
- **会议/期刊匹配**：根据论文草稿内容，推荐最合适的投稿目标

### 12.3 长期（v3.0+）
- **自动化科研**：Agent 自主设计实验 → 执行 → 分析结果 → 迭代优化 → 生成论文初稿
- **跨领域迁移发现**：识别其他领域的可迁移方法，提议跨领域创新
- **科研社交**：基于研究方向匹配潜在合作者
- **知识图谱**：构建领域知识图谱，支持更深层次的推理和发现

---

## 13. 附录：典型使用场景

### 场景 1：新手入门

> **用户**（研一新生）：我对伪造音频检测还不太了解，想快速了解这个领域。
>
> **Agent 流程**：
> 1. 确认研究方向为 "audio deepfake detection"
> 2. 自动检索 5 篇高引论文（覆盖经典到最新），展示概览
> 3. 一键入库 + 自动打标
> 4. 生成一份 5 页综述（按模型演进顺序组织）
> 5. 用户阅读综述后追问："AASIST 和 RawNet2 的核心区别是什么？"
> 6. Agent 基于知识库详细对比两个方法

### 场景 2：想法验证

> **用户**（博二学生）：我有个想法——用频域注意力机制来增强伪造音频检测的跨数据集泛化能力，帮我看看可行吗？
>
> **Agent 流程**：
> 1. RAG 检索知识库中关于 "频域"、"注意力机制"、"跨数据集泛化"、"伪造音频检测" 的相关论文
> 2. 展示检索到的相关论文，分析每篇与用户想法的关联
> 3. 多维度分析：
>    - 新颖性：有 3 篇论文涉及频域注意力，但没有专门针对跨数据集泛化的 → 有一定新颖性
>    - 可行性：频域特征提取是成熟技术，注意力机制有大量代码可用 → 技术可行
>    - 潜在难点：如何设计泛化验证协议、如何避免频域过拟合
> 4. 给出"可行（需注意泛化验证协议设计）"的结论
> 5. 展示差异化建议：结合频域注意力和域对抗训练可能是一个更好的组合

### 场景 3：方案生成

> **用户**（接上）：既然可行，帮我生成一份详细的实验方案，目标投 Interspeech，我只有一张 RTX 3090。
>
> **Agent 流程**：
> 1. 调用 generate_proposal 工具
> 2. 生成方案包括：
>    - 模型架构建议（在 AASIST 基础上增加频域注意力分支）
>    - 损失函数设计（EER 优化 + 域分类对抗损失）
>    - 数据集（ASVspoof2019 LA 训练，ASVspoof2021 LA/DF 泛化测试）
>    - 基线（RawNet2、AASIST、RawGAT-ST）
>    - 消融实验设计（频域注意力 on/off、对抗训练 on/off）
>    - 时间线（6 周实验 + 2 周论文）
> 3. 用户询问："频域注意力的具体实现方式有哪些？"
> 4. Agent 给出 2~3 种实现方案（频谱图注意力、MFCC 注意力、可学习滤波器组等）并分析优劣

### 场景 4：知识库扩充

> **用户**：我最近收集了几篇新论文的 PDF，帮我加入知识库。
>
> **Agent 流程**：
> 1. 用户拖拽上传 5 个 PDF
> 2. 对每个 PDF 计算 MD5，与知识库比对
> 3. 2 篇新论文 → 自动提取信息 + 打标 → 入库成功
> 4. 2 篇已存在 → 提示跳过
> 5. 1 篇标题相似 → 提示用户确认是否不同版本 → 用户确认为同一论文的新版本 → 更新已有条目
> 6. 入库完成后，Agent 提示："知识库现有 47 篇论文，是否要更新综述？"

---

## 文档版本记录

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-06-04 | 初始版本，完整需求文档 | — |

---

> **说明**：本文档为需求规格说明书，定义"做什么"（What），不涉及"怎么做"（How）。实现阶段的技术选型、架构设计、开发计划等将基于本需求文档另行制定。
